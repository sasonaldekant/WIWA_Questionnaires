SELECT 
    c.COLUMN_NAME,
    c.DATA_TYPE,
    c.CHARACTER_MAXIMUM_LENGTH,
    c.NUMERIC_PRECISION
FROM INFORMATION_SCHEMA.COLUMNS c
WHERE c.TABLE_NAME = 'QuestionnaireIdentificators'
ORDER BY c.ORDINAL_POSITION;
